# 2단원. API와 데이터 형식

## 학습 목표

- 딕셔너리(JSON 유사 구조)에서 필요한 값을 꺼낼 수 있다.
- 키 기반 접근 방식의 의미를 설명할 수 있다.

## 코랩 실습 코드

```python
sample = {"name": "Tom Nook", "species": "tanuki", "price": 98000}
print(sample["name"], sample["price"])
```

## 점검 질문

1. 키가 틀리면 어떤 오류가 발생하나요?
2. 여러 필드 중 핵심 필드만 선택하는 이유는 무엇인가요?
