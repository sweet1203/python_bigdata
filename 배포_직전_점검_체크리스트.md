# GOO's 데이터 분석 with 파이썬 — 배포 직전 점검 체크리스트

> 목적: 실제 수업 전 "열림/저장/이동/가독성" 문제를 사전에 차단한다.

---

## 1) 기본 실행 점검

- [ ] 로컬에서 페이지가 정상 실행된다. (`npm run dev`)
- [ ] 홈(`/`)에서 차시 카드·도움말 링크가 동작한다.
- [ ] 배포 URL에서도 동일한 화면/기능이 열린다.
- [ ] 콘솔 오류(빨간 에러)가 없는지 확인했다.

---

## 2) 라우트/메뉴 링크 점검

- [ ] 사이드바·하단 메뉴 링크가 모두 동작한다.
  - [ ] `/learn`
  - [ ] `/quiz`
  - [ ] `/progress`
  - [ ] `/help`
- [ ] `학습하기` 사이드바 단원 클릭 시 `?unit=` 전환이 정상 동작한다.
- [ ] 이전/다음 단원 버튼이 첫 단원/마지막 단원에서 올바르게 표시된다.

---

## 3) 학습/퀴즈/진도 저장 점검 (localStorage)

- [ ] `학습하기`에서 단원을 이동하면 방문 단원이 기록된다.
- [ ] `퀴즈`에서 답 제출 시 결과 메시지와 저장이 정상 동작한다.
- [ ] `내 학습 현황`에서 방문 단원 수/퀴즈 완료/정답 수가 반영된다.
- [ ] `진도 내보내기`로 JSON 파일이 내려받아진다.
- [ ] `진도 가져오기`로 JSON 파일을 다시 불러올 수 있다.
- [ ] `기록 초기화` 클릭 시 상태가 초기화된다.

---

## 4) 콘텐츠 점검

- [ ] 교안(차시별 마크다운)·퀴즈·실습 안내가 수업 흐름과 일치한다.
- [ ] 용어가 일관되다. (예: "학습하기", "내 학습 현황", "도움말")

---

## 5) 접근성/가독성 점검

- [ ] 키보드 `Tab`만으로 상단 메뉴와 주요 버튼 이동이 가능하다.
- [ ] 포커스 표시가 명확하게 보인다.
- [ ] "본문으로 건너뛰기" 링크가 키보드 탐색 시 동작한다.
- [ ] 모바일 화면(좁은 폭)에서 메뉴/본문이 잘리지 않는다.
- [ ] 학교 PC(일반 해상도)에서 글자 크기와 줄바꿈이 읽기 적절하다.

---

## 6) 수업 운영 최종 점검

- [ ] 첫 시간 안내 순서(학습하기 → 퀴즈 → 내 학습 현황)를 공지했다.
- [ ] 도움말 페이지의 안내 문구가 최신 수업 방식과 일치한다.
- [ ] 비상 대응 문구를 준비했다. (예: "로딩 실패 시 새로고침/재로그인")
- [ ] 학생에게 공유할 최종 URL을 확정했다.

---

## 7) 배포 승인 기록

- 점검 일시:
- 점검자:
- 최종 배포 URL:
- 특이사항:
- 승인 여부: ( ) 승인 / ( ) 보류
